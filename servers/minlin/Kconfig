menuconfig MINLIN_SERVER
    bool "Linux ABI compatibility layer (experimental)"
    select ABI_EMU

if MINLIN_SERVER

config AUTOSTART_MINLIN
    bool "Start automatically on boot"
    default y

endif
