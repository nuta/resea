menu "ARM64 Options"
	depends on ARCH_ARM64

    config TRACE_EXCEPTION
        bool "Trace exceptions (including system calls)"
        default n

endmenu

source "kernel/arch/arm64/machines/raspi3/Kconfig"
